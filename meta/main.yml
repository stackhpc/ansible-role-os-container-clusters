---
galaxy_info:
  role_name: container-clusters
  author: Mark Goddard
  description: >
    Role to register magnum container cluster resources
  company: StackHPC Ltd
  license: Apache2
  min_ansible_version: 2.0
  platforms:
    - name: EL
      versions:
        - 7
  galaxy_tags:
    - cloud
    - keystone
    - openstack

dependencies:
  - role: stackhpc.os-shade
    os_shade_venv: "{{ os_container_clusters_venv }}"
